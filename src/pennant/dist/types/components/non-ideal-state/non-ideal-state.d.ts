/// <reference types="react" />
import './non-ideal-state.scss';
export declare type NonIdealStateProps = {
  description?: string;
  title: string;
};
export declare const NonIdealState: ({
  description,
  title,
}: NonIdealStateProps) => JSX.Element;
